// import { useRecoilState } from "recoil";
// import type { UserProfile } from "@features/authentication/userProfile";
// import { userProfileState } from "@features/authentication/userProfile.atom";

// // use userProfile with Recoil
// export const useRecoilUserProfile = () => {
//   const [userProfile, setUserProfile] = useRecoilState(userProfileState);

//   const setUserProfileInfo = (info: UserProfile | null) => {
//     setUserProfile(info);
//   };

//   const clearUserProfile = () => {
//     setUserProfile(null);
//   };

//   return { userProfile, setUserProfileInfo, clearUserProfile };
// };
