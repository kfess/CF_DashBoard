import React from "react";
import Tooltip from "@mui/material/Tooltip";
import QuestionMarkIcon from "@mui/icons-material/QuestionMark";

type Props = {};

export const HelpTooltip: React.FC<Props> = (props: Props) => {
  const {} = props;

  return <></>;
};
